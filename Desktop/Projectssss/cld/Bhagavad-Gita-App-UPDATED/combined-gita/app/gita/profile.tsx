import { useRouter } from "expo-router";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { Screen } from "@/components/layout/Screen";
import { useAuth } from "@/lib/auth/AuthProvider";
import { colors, radii, spacing } from "@/constants/theme";
export default function ProfileScreen(){ const router=useRouter(); const {user,logout}=useAuth(); if(!user)return <Screen><Text style={styles.title}>Sign in to use your profile</Text><Pressable accessibilityRole="button" onPress={()=>router.push("/auth/login")} style={styles.button}><Text style={styles.buttonText}>Sign in</Text></Pressable></Screen>; return <Screen><View style={styles.card}><Text style={styles.title}>{user.displayName}</Text><Text style={styles.text}>{user.email}</Text><Text style={styles.badge}>{user.role === "ADMIN" ? "Administrator" : "Reader"}</Text><Text style={styles.text}>Your bookmarks, reading progress, language and theme preferences are kept separate from admin content controls.</Text><Pressable accessibilityRole="button" onPress={()=>void logout()} style={styles.button}><Text style={styles.buttonText}>Sign out</Text></Pressable></View></Screen> }
const styles=StyleSheet.create({card:{padding:spacing.xl,borderRadius:radii.lg,backgroundColor:colors.paper,borderWidth:1,borderColor:colors.border},title:{fontSize:28,fontWeight:"800",color:colors.ink},text:{color:colors.muted,fontSize:15,lineHeight:23,marginTop:spacing.sm},badge:{alignSelf:"flex-start",marginTop:spacing.md,paddingHorizontal:spacing.md,paddingVertical:spacing.xs,borderRadius:radii.pill,backgroundColor:colors.gold,color:colors.navy,fontWeight:"800"},button:{minHeight:50,borderRadius:radii.pill,backgroundColor:colors.navy,alignItems:"center",justifyContent:"center",marginTop:spacing.xl},buttonText:{color:colors.ivory,fontWeight:"800"}});
