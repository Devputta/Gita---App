/**
 * Database boundary.
 * Day 3 keeps the UI independent of the storage implementation.
 * The app currently uses the local structural repository; Supabase can be
 * wired behind the same ContentRepository interface after verification.
 */
export {};
